# 2026-07-30 GymFlow 架构改造

## 今日完成

### 1. ActiveWorkout.tsx 重构（511行 → ~100行）
- 引擎层 `useWorkoutEngine` 抽取：所有训练逻辑、计时器、AppState 监听
- `ExerciseBlock` 组件抽取：动作卡片 + 组编辑 + 横向进度条
- `PauseOverlay` 组件抽取：暂停遮罩 + 语音交互
- `ActiveWorkout` 瘦身为胶水层，只负责组合组件

### 2. 存储层抽象
- 新增 `src/db/types.ts` — `SessionStore`、`TemplateStore`、`SyncStore` 接口定义
- 新增 `src/db/stores.tsx` — `StoreProvider` + `useStores()` Context 注入
- `database.ts` 末尾加 `createStore()` 工厂，包装现有函数为接口对象
- 迁移 7 个文件从直接调 `database` → 走 `useStores()`
- 剩余直接引用仅两个基础设施文件（`_layout.tsx` 初始化、`supabase.ts` 云同步）

## 架构状态

```
app/ 路由层 → useStores() ← 接口定义
                          ↕
                    database.ts (SQLite 实现)
                    supabase.ts (云同步层)

src/hooks/    → useWorkoutEngine (useStores)
src/components/ → 纯 UI，不碰 database
```

## 后续路线图
1. ✅ ActiveWorkout 拆分完成
2. ✅ 存储层抽象完成
3. 📋 组件粒度细分（Timer、SetCard、WorkoutControls）
4. 🎯 app/ 路由层的 session 管理收敛
5. 🏗 Supabase 云同步接入（需实现 `SessionStore` 接口）
